import React from 'react'

export default function TestMultiple({theme}: {theme: string}) { // theme bisa bernilai 'ukm' atau 'hmif', bisa dimanfaatin buat logic milih warna
  return (
    <div>Test {theme}</div> 
  )
}
